import { antdConfirm } from "./antdConfirm"
import { antdMessage } from "./antdMessage"

/**
 * @name antd 通用方法
 * @example
 * antdMessage    //统一返回数据提示
 * antdConfirm    //统一返回信息提示框
 */
export const CSAntd = {
  antdConfirm,
  antdMessage
}
